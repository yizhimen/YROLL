项目	我现在的判断
Frame API	🟢 已经真正落地，但还没有完成 canonical 化
Selection	🟢 已有独立模型和测试
Revision	🟢 已落地
Edit Lease / Handoff	🟢 已落地，而且测试比较完整
No Silent Overwrite	🟢 基本成立
Time Mapping	🟡 有基础，但还不是完整的映射层
Mutation Engine	🟡 这是现在最大的核心问题
GUI	🔴 还不能因为 Core 完成就认为编辑器完成
Undo/Redo	🟡 有，但需要重新检查与 frame mutation 的一致性
Relationship propagation	🟡 已有不少旧逻辑，但还没统一到新的 mutation 模型
Preview / Impact	🔴 还没成为统一 mutation pipeline
Snap	🔴 还没有成为独立 canonical engine

仓库现在已经明确是 public，而且 yroll/core 已经拆出了 commands / models / project / server...，测试目录也已经增加了 test_frame_api.py、test_selection.py、test_lease.py、test_timebase.py 等，这说明这一轮确实不是“改了几个 API 名字”，而是在往基础层推进。

一、最重要的发现：Frame First 其实还没有真正完成

这是我现在最想提醒 Claude Code 的地方。

表面上：

add_clip_frame(...)
move_clip_frame(...)
trim_clip_frame(...)

已经存在。

测试也有。

例如目前的测试已经验证：

30fps
24fps
29.97fps
frame → sec → frame

这些都通过了。

但是从测试本身就能看到一个非常重要的事实：

assert c.timeline_range.start == 0.0
assert c.timeline_range.end == pytest.approx(2.0)

也就是说：

Frame API 目前仍然是“frame 输入 → 秒存储”。

而不是：

Timeline 本身就是 Frame-native。

这两者差别非常大。

二、这就是现在真正的 P0-01 → P0-02 边界

现在：

Agent
 ↓
add_clip_frame(0, 60)
 ↓
60 frames
 ↓
2.0 seconds
 ↓
TimeRange

看起来没问题。

但如果我们要做真正的：

帧编辑器

应该是：

Agent
 ↓
FrameTime(60)
 ↓
Canonical Timeline
 ↓
FrameRange(60, 120)

秒只是：

UI representation
Render adapter
External API adapter

而不是内部真相。

三、所以我现在不会让 Claude Code 重写

反而应该给它一个非常明确的任务：

不要重构现有 Frame API。

先找出当前工程里所有仍然以 float seconds 作为编辑语义的地方。

这是非常关键的一次 grep / audit。

重点找：

timeline_range.start
timeline_range.end
source_range.start
source_range.end
delta_seconds
position_seconds
duration_seconds
playhead seconds

然后分类：

A. 必须 frame-native

例如：

move
trim
split
ripple
insert
overwrite
snap
selection range
playhead editing position
B. 可以保留 seconds

例如：

asset.duration_sec
ffprobe metadata
audio measurement
external ffmpeg arguments
display
C. 必须做 Time Mapping

例如：

source → clip
clip → timeline
timeline → output

这才是我们真正需要的 P0-02。

四、commands.py 现在已经是一个非常大的“遗留核心”

这个我比较担心。

现在 commands.py 已经有 977 行，而且里面仍然混合着大量不同层次的操作。

比如现有代码里仍然能看到：

src_len = clip.source_range.end - clip.source_range.start

if src_len > 60:

以及：

freeze_sec

等等。

这些东西本身不是错误。

问题是：

Frame First 做完以后，不能让旧的 seconds-based mutation 和新的 frame mutation 长期并存。

否则最终一定会出现：

GUI → frame API
MCP → frame API
某个旧 command → seconds
Renderer → seconds
某个 ripple → seconds
Undo → seconds

然后你会发现：

“怎么明明都是 30fps，某几个操作还是会漂？”

这才是最危险的状态。

五、P0-04 我建议现在不要理解成“再写一个 Mutation Engine”

这一点我想修正一下之前的设计。

现在看实际代码后，我认为：

不需要另起一个巨大的 Mutation Engine。

因为 YROLL 已经有：

CommandLayer
Operation
ProjectCore
undo / redo

这些东西实际上就是 Mutation Engine 的雏形。

例如 ProjectCore 已经在根据 Operation.after 做 redo，并处理：

move
trim
volume
speed
transform
adjust
remove_clip
ripple_delete

等操作。

所以现在正确路线不是：

旧 CommandLayer
        ↓
删除
        ↓
新 MutationEngine

而是：

现有 CommandLayer
        ↓
统一 Frame semantics
        ↓
统一 Operation
        ↓
统一 Selection
        ↓
统一 Revision

也就是：

收敛，不推倒。
六、我现在认为真正的 P0-04 是这四件事

你刚才说：

剩下 7 个里有 4 个是 P0-04 的子任务

这个判断是对的。

我会重新定义成：

P0-04A — Frame-native mutation

把：

move
trim
split
delete

彻底接入 frame。

P0-04B — Selection mutation

从：

move_clip(clip_id)

升级成：

move_selection(selection, delta_frames)

然后：

single clip
multi clip
cross-track
linked clip

全部由同一条路径处理。

P0-04C — Impact / propagation

例如：

删除 Video

自动计算：

Subtitle
Voice
SFX
BGM
Relationship

谁跟、谁不跟。

P0-04D — Operation atomicity

一个用户动作：

删除一个镜头并 Ripple

必须成为：

ONE logical operation

而不是：

delete
shift subtitle
shift audio
shift video
...

然后 Undo 一次只撤掉一部分。

这个对于 Agent 尤其重要。

七、Selection 已经值得保留

我看了现在的测试结构，Selection 已经独立出来，而且测试也已经进入 repo。

所以这里不要重新设计。

下一步只需要问：

Selection 是否已经真正进入所有 mutation？

也就是说检查：

Selection
 ↓
CommandLayer
 ↓
Operation

是不是已经完整贯通。

如果只是：

Selection model exists
tests pass

但是实际：

move_selection()

内部还是：

for clip_id:
    move_clip(...)

那还只是半完成。

八、Lease 这一块，我反而认为已经做得不错

这个是这轮比较让我放心的地方。

测试已经覆盖：

acquire
release
heartbeat
conflict
handoff
mutation revision check

比如已经测试：

Human acquire
Agent acquire
→ 409

以及：

Human
 ↓
handoff
 ↓
Agent

的路径。

所以：

P0-10 不要继续折腾。

现在应该进入 GUI 表现层。

九、但 Edit Lease 还有一个非常重要的问题

目前 HTTP 层已经有：

/lease
/lease/acquire
/lease/release
/lease/handoff
/mutation/check

这很好。

但是我要检查的下一件事情不是 API 有没有。

而是：

每一个真正的 mutation endpoint 是否都强制经过 lease + revision。

这是两个完全不同的事情。

例如：

POST /lease/acquire

存在。

POST /mutation/check

存在。

但如果某个：

POST /clip/move

仍然：

CommandLayer.move_clip()
save_state()

那么 Lease 只是：

“旁边有一个锁系统。”

并没有真正保护工程。

所以这个必须做一次：

Mutation Gate Audit
十、我特别注意到了 server 仍然存在直接 save_state()

例如 chat websocket 的代码里仍然能看到：

result = task.run(ctx, req.message)

st.core.save_state()

这本身不一定错。

但我要 Claude Code 检查：

task.run() 里面的 mutation 是否已经经过统一的 revision / lease gate？

否则：

MCP
Chat
HTTP
GUI

可能还有不同的 mutation path。

这正是我们之前最担心的问题。

十一、Time Mapping 比我上一版想的更重要

我现在会把它提高到：

P0-02 = 当前第一优先级

而不是一个“以后再补的数学工具”。

因为你以后真正想让 Claude Code 做：

“把这一句最后一个字的画面延长 3 帧。”

它需要知道：

ASR word timestamp
       ↓
source frame
       ↓
clip local frame
       ↓
timeline frame

而不是：

0.733 sec
0.831 sec
0.1 sec

这种秒数层层相乘。

十二、我建议 Time Mapping 最终形成一个明确对象

例如：

TimeMap

负责：

source_frame → clip_frame
clip_frame → timeline_frame
timeline_frame → source_frame

并且考虑：

trim
speed
reverse
slip

以后再考虑：

nested sequence
transition
freeze

这样以后 Agent 才能真正问：

“Timeline 第 1832 帧对应原视频哪一帧？”

而 Core 能给它：

asset A
source frame 2214

这就是 AI-native editor 的基础。

十三、另外一个重要发现：现在的项目其实已经不是“Phase 0 Spike”了

README 还写着：

当前状态：Phase 0 技术验证 Spike

但现在代码已经明显进入：

Core
Commands
Server
MCP
GUI
Selection
Frame API
Lease
Revision
Render
Tests

而且测试已经有二十多个专门模块。

所以 README 现在已经落后于真实工程状态。

这不是大问题。

但我建议这轮基础层收敛之后马上改。

十四、现在不要再写 v0.3、v0.4、v0.5 一堆设计文档

这个我现在比之前更确定。

仓库里目前已经有很多历史文档：

Foundation Backlog
Reality Test
Sanlihe Gap
Editor Foundation Gap Analysis
产品蓝图
开发规划
补充文档
ChatGPT conversation
Session

GitHub 当前仍能看到这些并列存在。

所以现在最容易发生的事情就是：

Claude Code 看了五份文档，然后又“合理地”做出了第五种实现。

不要再这样。

十五、现在最适合做一个“施工状态审计”

我建议这次甚至不要马上让 Claude Code 改代码。

让它先做：

FOUNDATION_REALITY_AUDIT.md

只回答：

P0-01 Frame
    哪些已 frame-native
    哪些仍 seconds-native

P0-02 Time Mapping
    哪些已有
    哪些缺失

P0-03 Selection
    model
    command
    HTTP
    MCP
    GUI
    是否贯通

P0-04 Mutation
    move
    trim
    split
    delete
    ripple
    insert
    overwrite

P0-09 Revision
    所有 mutation endpoint 是否 enforced

P0-10 Lease
    所有 mutation endpoint 是否 enforced

P0-12
    是否存在任何 bypass
十六、尤其要让它画出这张图

这是我现在最想看到的：

                    GUI
                     │
                     ▼
                 HTTP API
                     │
                     ▼
                 CommandLayer
                     │
             ┌───────┴────────┐
             │                │
         Selection        TimeMap
             │                │
             └───────┬────────┘
                     ▼
                Mutation
                     │
             ┌───────┴────────┐
             │                │
         Revision          Lease
             │                │
             └───────┬────────┘
                     ▼
                 Operation
                     │
                     ▼
                 ProjectCore
                     │
                     ▼
                  State

然后把每一个现有 mutation command 的实际路径填进去。

这会比再读十篇设计文档有价值得多。

十七、所以现在我的施工顺序已经调整

我原来建议：

P0-01
P0-02
P0-03
P0-04

现在看完实际代码，我会变成：

第 0 步：不改代码

Foundation Reality Audit

第 1 步

P0-02 Time Mapping

但不是大重构。

建立：

TimeMap
FrameTime
FrameRange

然后逐渐让现有 mutation 使用它们。

第 2 步

P0-04A/B

把：

move
trim
split
delete
ripple

真正 frame-native + selection-native。

第 3 步

P0-04C/D

补：

Impact
Relationship propagation
Atomic operation
Undo/Redo
第 4 步

Revision + Lease 全链路 Gate Audit

这一步不是“再写功能”。

而是确认：

没有任何 mutation 可以绕过它们。

第 5 步

才进入：

Timeline GUI Reality Test

也就是你真正关心的：

我坐下来能不能像剪映一样剪？

十八、还有一个我现在特别想强调的判断

你之前说：

“Timeline 还是秒编辑器，不是帧编辑器。”

这句话现在更加成立。

但是我们不要把：

Frame API

误认为：

Frame Editor。

现在 YROLL 已经完成的是：

Frame-aware backend

距离：

Frame-native editor

还差一截。

而真正的终点应该是：

鼠标拖动
    ↓
Frame
    ↓
Snap
    ↓
Selection
    ↓
Mutation
    ↓
Impact
    ↓
Operation
    ↓
Revision
    ↓
Preview

而不是：

鼠标拖动
 ↓
0.133 sec
 ↓
某个 JS 算法
 ↓
后端再换成 frame
我的结论

现在绝对没必要重新做一版。

反而是：

这一轮已经把 YROLL 从“一个能跑的编辑器原型”推进到了“值得收敛编辑内核”的阶段。

现在最大的危险不是代码太烂。

而是：

新旧两套编辑语义并存太久。

尤其是：

Frame / Seconds、Selection / clip_id、Lease / mutation bypass、Operation / direct state mutation

这四组如果不收敛，后面 GUI 越做越漂亮，Agent 越做越强，问题反而越难修。

目前 YROLL 的 Foundation 实际上是：

P0	实际状态	我的判断
P0-01 Frame First	有 Frame 类型 + Frame API，但核心 Manifest/Commands 仍大量用 float seconds	🟡 半完成
P0-02 Time Mapping	有 fps/frame↔second 基础转换，但没有真正的 source↔clip↔timeline TimeMap	🔴 基本未完成
P0-03 Selection	Selection 对象和测试已经有了，但 Mutation 仍主要按 clip_id	🟡 表面完成 / 半完成
P0-04 Mutation Engine	已有很成熟的 CommandLayer + Operation，但还是旧的 seconds/clip-centric mutation	🟡 半完成，且是当前最大工作量
P0-09 Revision	revision 基础存在	🟢 基本完成，但要继续查 bypass
P0-10 Lease	acquire/release/handoff/heartbeat 都有	🟢 机制完成，强制接入还需审计
P0-12 No Silent Overwrite	有 revision check	🟢 基本完成，但不能只靠 /mutation/check

而且我需要修正上一轮我的一个判断：

我之前说 commands.py 是 977 行；现在 raw 文件实际已经是约 893 行。 说明 Claude Code 后续又动过它。GitHub 当前仓库也只有最近两次提交，确实是一个正在快速迭代中的状态。

一、P0-01：Frame First
结论：🟡 半完成

这部分最有意思。

YROLL 已经明确写下了正确的架构原则：

Internal canonical time is frames, not seconds.

而且已经有：

Rational
FrameTime
FrameRange
Timebase

FrameTime 也明确保存：

frame: int
fps: Rational

FrameRange 是标准的：

[start_frame, end_frame)

半开区间。

这部分设计是对的。

但是实际 Project 模型还没有迁过去

这里是一个非常关键的证据。

当前老的 Clip 仍然使用：

source_range: TimeRange
timeline_range: TimeRange

而 TimeRange 仍然是 float seconds 体系。

更直接的是 models.py / Manifest 体系本身仍是早期 Phase 0 模型，而 Shot.start/end 也是 float。

更严重的是，CommandLayer 的主编辑 API 仍然是 seconds-native。

例如：

def add_clip(
    asset_id,
    source_start: float,
    source_end: float,
    timeline_start: float,
)

然后直接：

TimeRange(
    start=source_start,
    end=source_end
)

以及：

TimeRange(
    start=timeline_start,
    end=timeline_start + duration
)

这说明：

真正的 canonical mutation 仍然是 seconds。

二、Frame API 是真的，但它现在更像 Adapter

这个测试特别能说明问题。

当前：

c = layer.add_clip_frame("fa", 0, 60, 0)

然后测试的结果却是：

assert c.timeline_range.start == 0.0
assert c.timeline_range.end == 2.0

也就是：

输入：
60 frames

↓

内部：
2.0 seconds

↓

Clip.timeline_range：
TimeRange(seconds)

这不是错误。

但它意味着：

Frame API 现在是 Frame → Seconds Adapter

还不是：

Frame-native Timeline

这就是你之前说：

Timeline 还是“秒编辑器”，不是“帧编辑器”。

现在从代码层面已经得到证实。

三、所以 P0-01 不应该再“增加更多 Frame API”

这是我现在非常明确的判断。

不要再让 Claude Code：

add_clip_frame()
move_clip_frame()
trim_clip_frame()
split_clip_frame()
...

继续无限增加。

否则最后会变成：

旧 seconds API
+
Frame API

两套系统。

正确做法是：

Frame
 ↓
Canonical Edit Model
 ↓
Command

然后 seconds API 只是：

UI / ffmpeg / import / export adapter
四、P0-02 Time Mapping：🔴 真正没完成

这里我现在比上一轮更确定。

目前 timebase.py 能做：

Frame → Seconds
Seconds → Frame

例如：

FrameTime.from_seconds(...)
FrameTime.to_seconds()

而且用了 Rational FPS，这一点是好的。

但是：

这不是我们真正需要的 Time Mapping。

真正需要的是：

Source Frame
      ↓
Clip Local Frame
      ↓
Timeline Frame

例如：

原素材：
Frame 12,000

Clip：
source = 10,000 ~ 13,000

Timeline：
frame = 1,500 ~ 4,500

那么：

source 12,000
    ↓
clip local 2,000
    ↓
timeline 3,500

这才是 Agent 真正需要的。

五、而现在字幕生成已经在“手写 Time Mapping”

这个地方非常关键。

generate_subtitles() 现在直接写：

tl_s = tr.start + (s - sr.start) / clip.speed
tl_e = tr.start + (e - sr.start) / clip.speed

也就是说：

Time Mapping 已经存在。

只是它没有成为独立的基础设施。

它散落在 CommandLayer 里面。

这就是典型的：

功能有了，抽象没有。

以后一旦加入：

reverse
speed ramp
slip
freeze
nested sequence
transition

这里会越来越乱。

六、所以 P0-02 应该不是“大数学模块”

只需要建立一个明确的：

TimeMap

它至少负责：

source_frame → timeline_frame
timeline_frame → source_frame
source_frame → clip_frame
clip_frame → timeline_frame

然后：

speed
trim
slip
reverse

全部通过它。

这会成为后面 Agent 精确剪辑的关键基础。

七、P0-03 Selection：🟡 表面完成 → 半完成

这个我给 Claude Code 的评价其实不错。

现在已经有真正独立的：

@dataclass
class Selection:

并且支持：

single clip
multi clip
range
track

还有：

intersects()
contains_clip()
contains_track()

测试也覆盖了这些情况。

这部分不要推倒重做。

八、但是 Selection 的注释已经暴露了现实状态

文件顶部直接写着：

every mutation should accept it instead of just clip_id

这句话本身就说明：

现在 Mutation 还没有做到。

当前：

move_clip(clip_id, ...)
trim_clip(clip_id, ...)
split_clip(clip_id, ...)
remove_clip(clip_id)

仍然是主 API。

也就是说：

Selection Model
        ↓
        X
Command Layer

还没有真正贯通。

九、所以 P0-03 的真实状态应该改成
Selection object       ✅
Selection tests        ✅
FrameRange selection   ✅
Selection HTTP/MCP     🟡
Selection → mutation   🔴
Multi-selection edit   🔴
Range selection edit   🔴
Track selection edit   🔴

所以不能说 P0-03 已完成。

准确说：

P0-03A 完成，P0-03B 未完成。
十、P0-04 Mutation Engine：这里才是现在真正的“战场”

现在 YROLL 其实已经有一个不错的 Mutation Engine 雏形：

CommandLayer
    ↓
before / after
    ↓
Operation
    ↓
ProjectCore.log()

这一点架构是正确的。

文件顶部甚至已经明确规定：

GUI、Agent、MCP 外部工具调用完全相同的 Command API。

并且每个 Command：

validate
calculate before/after
modify Project
log Operation

这部分不要推倒。

十一、但是现在 CommandLayer 里面有两代编辑系统

这才是最大的问题。

第一代：

seconds
clip_id
TimeRange

第二代：

frames
Selection

两套并存。

例如：

add_clip()
trim_clip()
split_clip()
move_clip()
ripple_delete_clip()

全部还是 seconds / clip-centric。

而新增：

add_clip_frame()
move_clip_frame()
trim_clip_frame()

又在另一边。

所以现在实际上是：

                  CommandLayer
                 /            \
        Legacy commands       Frame commands
             │                     │
          seconds                frames
             │                     │
         clip_id               clip_id

这就是我们现在最需要消灭的东西。

十二、Mutation 还有一个更严重的问题：Operation 不是完整的“事务”

当前 Operation 的设计很好：

before
after
who
why
tool
time_range

但是某些 mutation 实际修改的东西比 Operation 表达的更多。

最典型：

Ripple Delete

它会：

删除一个 clip
+
移动同轨后续 clips
+
移动跨轨 strong-linked clips

代码里确实记录了：

shifted
cross_shifted

这是好的。

但这已经说明：

一个 Operation 实际上是一个复合 mutation。

未来我们应该把它明确成为：

Operation
  ├─ primary change
  ├─ affected objects
  ├─ timeline delta
  └─ relationship impacts

而不是靠 before 字典临时塞东西。

十三、Undo/Redo 也存在这个问题

ProjectCore 已经有相当完整的 undo/redo。

例如：

trim
move
speed
ripple_delete
split
remove_clip
add_clip
...

都有对应恢复逻辑。

但是它现在仍然是：

按 Operation type 写一大串 if/elif 的恢复器。

也就是说：

if op_type == "trim":
if op_type == "move":
if op_type == "ripple_delete":
if op_type == "split":
...

随着 mutation 数量增长，这会越来越难维护。

这不是 P0-04 必须马上重构掉的东西。

但是我们需要确保：

所有新的 frame-native mutation 都能产生可逆的 Operation。

否则 Frame First 最后会变成：

编辑能按帧
Undo 又回到 seconds 逻辑
十四、这里发现一个很重要的 bypass

这个比我之前预想的更值得注意。

replace_clip_voice() 内部直接：

self.add_clip(...)
self.set_muted(...)
self.core.save_state()

然后自己再：

return self._record(...)

也就是说：

一个高级 Command 内部调用多个 Command，并且自己还直接 save_state()。

这意味着 Operation 的“原子性”现在还不是严格的。

可能产生：

Operation A
  add_clip

Operation B
  mute

Operation C
  voice_replace

或者：

save_state()

发生在最终 Operation 之前。

这就是一个典型的：

Mutation composition / transaction boundary 问题

不一定马上出 bug，但必须列入 P0-04。

十五、另一个明显的 seconds bypass：silence / filler

例如：

"removed_seconds"

以及：

min_word_dur: float

内部还是秒。

这本身不一定需要改。

因为：

媒体分析的原始时间可以是 seconds。

真正的问题是：

分析结果
 ↓
mutation

必须在进入编辑内核的时候转换成 frame。

现在还没有形成这个边界。

十六、Revision / Lease：🟢 机制已经比较扎实

这一块我不建议 Claude Code 再大改。

当前 server 已经有：

/lease/acquire
/lease/release
/lease/heartbeat
/lease/handoff
/mutation/check

并且 mutation check 会：

require_edit_right()
+
check_revision_match()

测试也覆盖：

acquire
release
conflict
handoff

等路径。

所以：

P0-10 的核心机制是真的完成了。
十七、但是 Lease 最大的问题仍然是“Gate”

这里我要特别谨慎。

目前有：

@app.post("/mutation/check")

这不代表：

所有 mutation 都必须经过它。

例如 server 仍然大量存在：

return guard(lambda: st.cmd.xxx(...))

这种路径。

因此现在必须做：

Mutation Gate Audit

不是看：

“有没有 lease API？”

而是逐个检查：

HTTP mutation
MCP mutation
Chat mutation
GUI mutation
Internal Task mutation

最终是否都经过：

Lease
  ↓
Revision
  ↓
Command
  ↓
Operation
  ↓
Save

如果存在：

Command
 ↓
save_state()

但没有：

Lease / Revision

那就是 bypass。

十八、Chat Agent 是现在最值得检查的一条路径

这里尤其明显：

task = Task(
    CommandLayer(st.core, who=Actor.AI),
    ...
)

result = task.run(...)
st.core.save_state()

也就是说：

Chat
 ↓
Task
 ↓
CommandLayer
 ↓
save_state

这条路径存在。

问题不是它一定有 bug。

问题是：

Task 是否在执行 Command 前强制检查 Lease + Revision？

从 server 这一层看不到这个 gate。

所以这必须让 Claude Code 做一次静态追踪。

十九、WS Chat 还有同样的问题

WebSocket 路径最终：

task.run(...)
...
st.core.save_state()

或者：

task.apply_actions(...)
...
st.core.save_state()

所以现在真正应该测试的是：

人持有 Edit Lease 时，Agent Chat 能不能绕过 Lease 修改工程？

这个测试非常重要。

如果：

Human Lease
+
Agent Chat
→ modification succeeds

那就是 P0-10 真正的 bypass。

二十、我现在给整个 P0-01～04 画成这样
                     CURRENT YROLL

                 ┌──────────────────┐
                 │       GUI        │
                 └────────┬─────────┘
                          │
                    HTTP / MCP
                          │
                          ▼
                 ┌──────────────────┐
                 │  CommandLayer    │
                 │                  │
                 │ ┌──────────────┐ │
                 │ │ Legacy       │ │
                 │ │ seconds      │ │
                 │ │ clip_id      │ │
                 │ └──────────────┘ │
                 │        +         │
                 │ ┌──────────────┐ │
                 │ │ New Frame    │ │
                 │ │ frame API    │ │
                 │ └──────────────┘ │
                 └────────┬─────────┘
                          │
                          ▼
                 ┌──────────────────┐
                 │   ProjectCore    │
                 │                  │
                 │ TimeRange        │
                 │ Operation        │
                 │ undo/redo        │
                 └────────┬─────────┘
                          │
                          ▼
                       State

真正应该变成：

                     TARGET

              GUI / Agent / MCP
                       │
                       ▼
                Selection
                       │
                       ▼
                  TimeMap
                       │
                       ▼
              Frame-native Command
                       │
                       ▼
                Mutation Plan
                       │
             ┌─────────┴─────────┐
             ▼                   ▼
          Impact              Operation
             │                   │
             └─────────┬─────────┘
                       ▼
                   ProjectCore
                       │
                Revision + Lease
                       │
                       ▼
                     State
二十一、所以我现在给它们重新定级
🟢 真完成
Selection 基础模型
Selection
FrameRange
single/many/track/range
tests

已经可以保留。

Lease 基础设施
acquire
release
heartbeat
handoff
conflict

已经可以保留。

Operation / Undo 基础设施

虽然不完美，但已经足够作为 Foundation，不值得推倒。

二十二、🟡 半完成
P0-01

Frame API 完成 ≠ Frame-native Core 完成。

需要：

Clip TimeRange
↓
FrameRange

Mutation
↓
frames

UI
↓
frames

逐步收敛。

P0-03

Selection model 完成。

但：

Selection
 ↓
Mutation

还没真正接通。

P0-04

CommandLayer 已经是一个不错的基础。

但：

seconds + clip_id

仍是主路径。

需要把它迁成：

frames + Selection
二十三、🔴 真正缺失
P0-02 TimeMap

这是现在最明确的缺口。

不是缺：

frame ↔ sec

而是缺：

source ↔ clip ↔ timeline
二十四、🔴 目前最值得警惕的 bypass

我现在列出四类：

Bypass 1
Legacy seconds mutation

仍然可以直接修改工程。

Bypass 2
高级 Command
 ↓
调用其他 Command
 ↓
save_state()
 ↓
自己再记录 Operation

replace_clip_voice() 就是典型例子。

Bypass 3
Chat Task
 ↓
CommandLayer
 ↓
save_state

需要验证是否经过 Lease/Revision。

Bypass 4
HTTP endpoint
 ↓
guard()
 ↓
Command

如果 guard() 只负责异常处理，而不负责统一 mutation gate，那么就存在绕过风险。

这个必须继续查。

二十六、下一张施工单应该非常具体

我建议下一阶段只给 Claude Code 一个任务：

FOUNDATION_REALITY_AUDIT + MIGRATION PLAN

不要让它改大量代码。

要求它产出一份：

P0-01-04-REALITY-AUDIT.md

内容必须包含：

1. 所有 mutation command
2. 每个 command 当前时间单位
3. 每个 command 是否接受 Selection
4. 每个 command 是否接受 Frame
5. 每个 command 是否经过 Operation
6. 每个 command 是否经过 Revision
7. 每个 command 是否经过 Lease
8. 每个 command 是否直接 save_state
9. 每个 command 是否存在内部 Command 嵌套
10. 每个 command 是否存在 seconds bypass

然后生成一张表：

Command	时间	Selection	Operation	Revision	Lease	Save	状态
add_clip	sec	❌	✅	?	?	?	🟡
add_clip_frame	frame→sec	❌	✅	?	?	?	🟡
move_clip	sec	❌	✅	?	?	?	🟡
move_clip_frame	frame→sec	❌	✅	?	?	?	🟡
trim_clip	sec	❌	✅	?	?	?	🟡
split_clip	sec	❌	✅	?	?	?	🟡
ripple_delete	sec	❌	✅	?	?	?	🟠
silence_remove	sec	❌	✅	?	?	?	🟠
filler_remove	sec	❌	✅	?	?	?	🟠

这张表一旦出来，我们就不用再猜 YROLL 到底完成了多少。

二十七、然后才是正式施工顺序

我现在会把顺序定死：

Phase A

P0-02 TimeMap

先解决：

source
clip
timeline

之间的 frame mapping。

↓

Phase B

P0-01 Canonical Frame Core

不是再增加 *_frame() API。

而是让：

Frame

成为真正的编辑语义。

↓

Phase C

P0-03 Selection → Mutation

让：

move(selection, delta_frames)
trim(selection, ...)
delete(selection)

成为正式 API。

↓

Phase D

P0-04 Mutation

统一：

Selection
+
Frame
+
TimeMap
+
Impact
+
Operation

↓

Phase E

Mutation Gate

统一：

Lease
+
Revision

并消灭所有 bypass。

最后一个非常重要的判断

这次实际代码审计以后，我反而比之前更看好 YROLL。

因为最麻烦的东西其实已经出现了：

Command Layer 有了；
Operation 有了；
Undo/Redo 有了；
Frame primitives 有了；
Selection 有了；
Lease 有了；
Revision 有了；
MCP/HTTP/GUI 的统一方向已经明确。

现在的问题不是：

“YROLL 要不要重做？”

而是：

把已经存在的几套半成品语义，收敛成一条真正唯一的编辑路径。

这也是为什么我现在不建议继续加功能。

先把这条线打通：

Frame
  ↓
TimeMap
  ↓
Selection
  ↓
Mutation
  ↓
Impact
  ↓
Operation
  ↓
Revision + Lease
  ↓
Project State

一旦这条链真正成立，后面你之前说的那个目标——Claude Code / Agent 不再只是“生成剪映草稿”，而是真正可以精确到帧地理解、修改、预览、撤销一个视频工程——才算有了可靠的地基。

而目前仓库的 README 还把项目描述为“Phase 0 技术验证 Spike”，这已经明显落后于实际代码状态，也说明现在确实应该进入一次Foundation 收敛阶段了。

YROLL 现在最大的风险不是 Frame，而是 Mutation Gate

Frame 这边是“架构尚未收敛”。

但 Lease / Revision 这边已经出现了真正的 bypass：

有些 HTTP mutation 有 revision 检查，有些没有；有些能检查 session lease，有些完全不检查。

甚至 /mutation/check 本身存在，并不意味着 mutation 自动受它保护。

这件事应该优先修。

一、P0-04 实际 Mutation 全路径审计

我把目前 commands.py 中真正影响时间线/工程状态的操作分成四组。

A. 核心时间线编辑
Mutation	当前时间语义	Selection	Frame	Operation	当前评价
add_clip	seconds	❌	Adapter	✅	🟡
remove_clip	seconds	❌	❌	✅	🟡
ripple_delete_clip	seconds	❌	❌	✅	🟠
trim_clip	seconds	❌	Adapter	✅	🟡
split_clip	seconds	❌	Adapter	✅	🟡
move_clip	seconds	❌	Adapter	✅	🟡
set_speed	seconds	❌	❌	✅	🟠

这里已经可以看到我们一直说的核心问题：

所有真正的核心 mutation 仍然以 TimeRange(float seconds) 为中心。

例如 trim_clip() 直接修改：

source_range.start/end
timeline_range.start/end

都是 float seconds。split_clip() 也是根据 source seconds 计算比例，再生成新的 seconds-based TimeRange。move_clip() 同样如此。

所以目前新增的：

add_clip_frame()
move_clip_frame()
trim_clip_frame()
split_clip_frame()

本质上仍然只是：

Frame
 ↓
_frame_to_sec()
 ↓
旧 seconds mutation

代码甚至直接写明：

Frame-based add_clip. Internally converts to seconds.

所以 P0-01 我现在正式定性为：

🟡 Frame API 完成，Frame-native Core 未完成
二、P0-02 Time Mapping：现在可以明确判定为缺失

目前 YROLL 有：

frame → second
second → frame

但真正的视频编辑 TimeMap 应该是：

Source Frame
     ↓
Clip Local Frame
     ↓
Timeline Frame

现在这件事情已经被代码重复实现了好几遍。

例如字幕：

source timestamp
    ↓
source_range
    ↓
timeline_range
    ↓
speed

直接计算：

tl_s = tr.start + (s - sr.start) / clip.speed

填充词删除又自己做了一套 source → timeline 映射。

所以：

Time Mapping 不是没有代码，而是没有成为基础设施。

这是非常重要的区别。

三、P0-03 Selection：基础完成，Mutation 接不上

Selection 本身我继续维持之前的判断：

🟢 Selection Model

已经值得保留。

但实际 mutation 还是：

move_clip(clip_id)
trim_clip(clip_id)
remove_clip(clip_id)

而不是：

move(selection)
trim(selection)
delete(selection)

所以现在真实状态是：

Selection
   ↓
   X
Command Layer

而不是：

Selection
   ↓
Mutation

因此：

🟡 P0-03 = Model 完成，Mutation integration 未完成
四、P0-04：Ripple Delete 是目前最值得重做的一个 Mutation

ripple_delete_clip() 现在实际上已经做了不少正确事情：

找主 clip
删除
同轨后续 clip 前移
根据 Semantic Link 找 strong relationship
跨轨字幕/音频一起移动
最后记录一个 ripple_delete Operation

这说明设计思路已经很好。

但是它现在仍然是：

seconds
+
clip_id
+
直接修改多个 Clip

而不是：

Selection
+
FrameRange
+
Impact Plan
+
Atomic Operation

所以它其实已经是未来 Mutation Engine 的雏形。

不要推倒。

应该把它升级。

五、Ripple Delete 的 Operation 也暴露了一个结构问题

现在：

before:
    clip
    shifted
    cross_shifted

after:
    shifted_count
    cross_shifted_count

也就是说真正的修改对象在 before 里。

Redo 又重新根据：

old_start
dur
shifted
cross_shifted

推算。

这能工作。

但对于以后 Agent 来说，我更希望变成：

MutationPlan

primary:
    delete clip X

effects:
    shift clip A -12 frames
    shift clip B -12 frames
    shift subtitle C -12 frames

operation:
    atomic

这样 Agent 才能在执行之前看到：

“我准备删除这个镜头，会影响 7 个对象。”

这就是我们之前说的 Preview / Impact。

六、remove_silence：这是一个更大的 Mutation

这个现在已经不是简单的：

trim

而是：

一个 Clip
 ↓
检测 silence
 ↓
得到多个 keep ranges
 ↓
原 Clip 改成第一段
 ↓
创建 N 个新 Clip
 ↓
重排 track

所以它其实已经是一个：

Composite Mutation

但目前仍然只返回一个 silence_remove Operation。

这个没问题。

问题是：

它未来必须成为一个真正 atomic mutation。

否则 Undo/Redo、Revision、Preview、Agent approval 都会越来越难处理。

七、remove_filler_words：同样是 Composite Mutation

这个甚至更复杂。

它：

ASR words
 ↓
找 filler
 ↓
source → timeline
 ↓
生成 filler ranges
 ↓
合并
 ↓
删除原 clip
 ↓
创建多个新 clip

所以它实际上也是：

N mutations

但对用户来说应该是：

ONE mutation

这个原则应该成为 v0.2 Foundation 的正式规定：

一个用户/Agent 意图 = 一个 Atomic Operation。

八、speed 现在是一个很容易被忽略的 Frame 问题

当前：

clip.speed = speed
clip.timeline_range.end =
    clip.timeline_range.start + src_len / speed

全部还是 seconds。

而以后如果我们有：

30fps
source 101 frames
speed 1.5x

timeline 到底是：

67 frames

还是：

67.333...

这个必须由 TimeMap / Frame policy 决定。

所以：

speed 不能最后才迁移。

它是 P0-02 TimeMap 的测试案例。

九、字幕也是一个重要的 Frame-native 测试案例

目前：

ASR
 ↓
seconds
 ↓
source range
 ↓
speed
 ↓
timeline seconds

以后真正应该：

ASR timestamp
 ↓
SourceTime
 ↓
TimeMap
 ↓
Timeline FrameRange
 ↓
Subtitle Clip

所以我建议：

TimeMap 第一批测试不要拿普通 move/trim 做，而拿“字幕映射 + speed + trim”做。

因为这能很快暴露映射层设计是不是正确。

十、然后我找到一个非常明确的 Revision Bypass

这件事非常重要。

目前 server 中：

add_clip

有：

sessionId
baseRevision

并且：

sessionId → require_edit_right()
baseRevision → check_project_revision()

trim

有：

baseRevision
split

有：

baseRevision
move

有：

baseRevision

但是：

speed

没有。

POST /clips/{clip_id}/speed

直接：

guard(
    st.cmd.set_speed(...)
)

volume

没有。

adjust

没有。

silence-remove

没有。

denoise

没有。

delogo

没有。

transform

没有。

color

没有。

flip

没有。

opacity

没有。

crop

没有。

reverse

没有。

fade

没有。

dissolve

也没有。

也就是说现在的 Revision 保护实际上是：

“部分 mutation 有 Revision”

而不是：

“Mutation Engine 有 Revision”

这两个差别非常大。

十一、更严重的是 Lease Bypass

这个比 Revision 更重要。

现在：

/mutation/check

确实可以：

require_edit_right()
+
check_revision_match()

但是：

它是一个 endpoint，不是 mutation middleware。

十二、最明显的证据就在 /tracks

例如：

POST /tracks

直接：

st.cmd.add_track(...)

没有：

sessionId
require_edit_right
baseRevision

然后：

POST /tracks/{track_id}/mute
POST /tracks/{track_id}/lock
POST /tracks/{track_id}/hide

同样没有统一 mutation gate。

十三、甚至 clip 的很多视觉操作也没有 Gate

例如：

transform
color
flip
opacity
crop
reverse
transform2d
reset-visual
fade
dissolve

都是：

guard(
    command(...)
)

没有统一：

Lease
+
Revision

所以现在真的存在我们之前担心的情况：

Human
 ↓
拿着 Edit Lease

Claude Code
 ↓
调用某个未保护 endpoint
 ↓
CommandLayer
 ↓
修改 Project

这是一个真实的 Foundation 问题，不是理论问题。

十四、Chat Agent 是另一个明确的 bypass 候选

这个更加关键。

当前：

task = Task(
    CommandLayer(st.core, who=Actor.AI),
    ...
)

result = task.run(...)
st.core.save_state()

WebSocket Chat 也是：

Task(CommandLayer(...))
 ↓
task.run()
或
task.apply_actions()
 ↓
st.core.save_state()

这里没有看到 Lease / Revision gate。

所以必须把它当成：

🔴 P0-10 / P0-12 高优先级验证项
十五、这意味着目前真正存在两条 Agent 编辑路径
                    AI
                   /  \
                  /    \
             HTTP       Chat Task
               │           │
               ▼           ▼
          部分 Gate       CommandLayer
               │           │
               └─────┬─────┘
                     ▼
                 Project

而我们真正需要的是：

                    AI
                   /  \
                  /    \
             HTTP       Chat
               │           │
               └─────┬─────┘
                     ▼
              Mutation Gate
                     │
             ┌───────┴────────┐
             │                │
           Lease          Revision
             │                │
             └───────┬────────┘
                     ▼
                 Command
                     ▼
                 Operation
                     ▼
                  Project
十六、还有一个非常值得修的东西：require_revision() 现在实际上没用

server 里有：

def require_revision(fn):

但里面实际上没有完成真正的检查逻辑。

真正使用的是：

_check_rev(...)

这就是典型的：

旧 Foundation 遗留代码。

应该清掉或者统一。

否则以后 Claude Code 又会：

“这里已经有 require_revision 啊。”

然后误以为系统已经统一。

十七、replace_clip_voice() 是目前最典型的 Atomicity Bypass

这个我再次确认了。

它：

TTS
 ↓
创建 Asset
 ↓
add_clip()
 ↓
set_muted()
 ↓
save_state()
 ↓
_record("voice_replace")

也就是：

voice_replace

这个用户动作内部实际上产生：

add_clip Operation
mute Operation
voice_replace Operation

而且中间还：

save_state()

这非常不适合作为未来 Agent 的原子操作。

应该改成：

voice_replace
 ├── create asset
 ├── add generated audio clip
 ├── mute original
 └── ONE Operation

或者建立真正的：

Transaction / MutationBatch
十八、所以现在 P0-04 的真实结构，我建议改成这样

不是再造一个“巨大 Mutation Engine”。

而是在现有 CommandLayer 上加：

MutationContext

例如概念上：

MutationContext
├── actor
├── session
├── base_revision
├── selection
├── timebase
└── reason

然后：

Command
   ↓
MutationContext
   ↓
validate
   ↓
plan
   ↓
apply
   ↓
Operation
   ↓
commit

这样我们原来的 CommandLayer 不用推倒。

十九、我现在给所有 mutation 做最终评级
🟢 可以保留
Operation
CommandLayer
Undo/Redo
Selection model
FrameTime / FrameRange
Lease store
Revision model

这些都已经值得作为基础。

🟡 必须迁移
add_clip
trim
split
move
remove
ripple_delete
speed
subtitle generation

原因：

seconds-native。

🟠 必须升级为 Atomic Composite Mutation
ripple_delete
remove_silence
remove_filler_words
replace_clip_voice
🔴 必须修 Gate
track mutations
visual mutations
audio mutations
speed
adjust
silence remove
AI Chat

原因：

当前没有统一 Lease + Revision enforcement。

二十、最终我认为现在应该给 Claude Code 的施工顺序已经非常明确
P0-04A：先不要改 Frame

第一步：

Mutation Gate

把所有 mutation 统一经过：

require_edit_right()
+
check_project_revision()

而且不能让调用方自己选择：

sessionId=""

来绕过。

需要明确：

编辑请求必须带 mutation context。

P0-04B：Atomic Mutation

解决：

replace_voice
silence_remove
filler_remove
ripple_delete

让：

一个用户意图
=
一个 Operation

并禁止内部随意：

save_state()
P0-02：TimeMap

然后建立：

SourceFrame
ClipFrame
TimelineFrame

的 canonical mapping。

P0-01：真正 Frame-native

再把：

trim
move
split
ripple
speed

从：

seconds

迁移到：

frames

而不是继续增加 *_frame() adapter。

P0-03：Selection integration

最后：

Selection
 ↓
Mutation

真正贯通。

二十一、我建议 Claude Code 下一轮不要“写代码”，而是先做这个

直接给它一个非常明确的任务：

暂停新增功能。只完成 YROLL Foundation Mutation Audit + Gate Test。不要重构 Frame Core。

让它新增测试：

tests/test_mutation_gate.py

至少测试：

Test 1
Human lease
+
AI mutation
→ 409
Test 2
AI lease
+
Human mutation
→ 409
Test 3
stale baseRevision
+
任何 mutation
→ 409
Test 4
当前 revision
+
任何 mutation
→ success
Test 5

把所有 mutation endpoint参数化测试：

add_clip
add_track
remove_clip
ripple_delete
trim
split
move
speed
volume
adjust
transform
color
flip
opacity
crop
reverse
fade
dissolve
silence_remove
denoise
delogo
...

确保：

没有任何 mutation 可以绕过 Gate。
二十二、而且我发现一个特别适合 YROLL 的设计

你刚才提到的：

“状态暂时冻结/交接，类似 git，而且要让人能直观看到。”

现在我们实际上已经有了：

Revision
+
Lease
+
Operation
+
Version

所以不用再造一个完全不同的系统。

可以最终形成：

                YROLL Edit Session

             ┌───────────────────┐
             │  EDIT LEASE       │
             │                   │
             │ 👤 Human          │
             │ Revision: 183     │
             │                   │
             │ [ HANDOFF TO AI ] │
             └───────────────────┘

当 AI 接管：

             ┌───────────────────┐
             │  AI EDITING       │
             │                   │
             │ 🤖 Claude Code    │
             │ Revision: 184     │
             │                   │
             │ Human: READ ONLY  │
             │                   │
             │ [ HANDOFF BACK ]  │
             └───────────────────┘

GUI 时间线直接：

黄色/灰色状态条 + “AI 正在编辑 · Revision 184”

这就真正实现你之前说的：

像 Git 一样交接，但普通用户一眼就能看懂。

而现在后端已经有 Lease/Handoff 的基础，所以这是下一阶段 GUI 的自然表现层，不是重新发明基础设施。

二十三、现在这轮审计之后，我给 YROLL v0.2 的真实状态重新定一个数字

不是：

5/12 P0 完成

而是：

Foundation Core

约 60% 的结构已经有了。

真正可依赖的部分

约 40–50%。

最大缺口

不是功能数量。

而是：

现有功能还没有收敛成唯一、强制、frame-native 的 mutation path。

这其实是个好消息。

因为如果是“功能什么都没有”，我们得做半年。

现在是：

东西已经很多
 ↓
把它们收拢
 ↓
堵 bypass
 ↓
统一 Frame
 ↓
统一 Selection
 ↓
统一 Mutation

这比重新做一个编辑器容易得多。

所以，我建议现在正式进入这个阶段
YROLL Editor Foundation v0.2 — Mutation Integrity Phase

第一施工目标只有三个：

① Mutation Gate
   Lease + Revision 全覆盖

② Atomic Mutation
   一个意图 = 一个 Operation

③ TimeMap
   Source ↔ Clip ↔ Timeline

暂时不要再让 Claude Code 加 GUI 功能、加新剪辑功能、加更多 Frame API。

等这三个真正通过测试，再进入下一轮：

Frame-native Mutation → Selection-native Editing → Timeline GUI Reality Test

这时候 YROLL 才开始从“有很多编辑功能的 Python 项目”，真正变成我们想要的那个Agent-native NLE 编辑内核。

而且这次我建议不要再写一堆新的设计文档。现有仓库里现在仍然同时保留 YROLL-Editor-Foundation-Backlog-v0.1、Gap Analysis、Reality Test、开发规划等多份历史文件。 等这轮施工完成，我们就把这些历史文档收束成唯一的《YROLL Editor Foundation v0.2》施工依据。